#include<stdio.h>
#include<string.h>

//串的堆分配存储表示
typedef struct s{
	char *ch;
	int length;
} HString;

//注意在串的操作中，第一步判断要返回的HString类型结构体的ch指针是否不为空。
//若不为空，free掉  从而可以重新赋予其新的值


int main(){
	
	return 0;
}





concat
substr
等等的意义搞清楚